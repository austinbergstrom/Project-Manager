<%= form_for(@skill_level) do |f| %>
  <% if @skill_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_level.errors.count, "error") %> prohibited this skill_level from being saved:</h2>

      <ul>
      <% @skill_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
