<h1>Listing zones</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Responsibility type</th>
    <th>Service type</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Level</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @zones.each do |zone| %>
  <tr>
    <td><%= zone.name %></td>
    <td><%= zone.responsibility_type_id %></td>
    <td><%= zone.service_type_id %></td>
    <td><%= zone.start_date %></td>
    <td><%= zone.end_date %></td>
    <td><%= zone.level %></td>
    <td><%= link_to 'Show', zone %></td>
    <td><%= link_to 'Edit', edit_zone_path(zone) %></td>
    <td><%= link_to 'Destroy', zone, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Zone', new_zone_path %>
